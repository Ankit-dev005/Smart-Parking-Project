spring:
  application:
    name: smart_parking_system
  datasource:
    url: jdbc:mysql://localhost:3306/smart-parking?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: WJ28@krhps
    driver-class-name: com.mysql.cj.jdbc.Driver		
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql:true
logging:
  level:
    org:
      springframework:
        security: DEBUG
server:
  port: 1997